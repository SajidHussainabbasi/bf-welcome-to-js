'use strict';

let input = null;
while (true) { // infinite loop
  input = prompt('you are stuck! input something:');
}

alert('this will never be reached');

/*  ===== Challenges =====
  - Try to create an infinite loop so the user gets stuck inside your prompt.
*/

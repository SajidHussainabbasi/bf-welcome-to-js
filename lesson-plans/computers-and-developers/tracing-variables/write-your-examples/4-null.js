'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
//Create variable(null) job and initialized
let job = null;
// Read the variable job
console.log(job);
//Create const variable(null) var1 and initialized
const var1 = null;
// create a if condition to use variable(null),initialize and read the variable
if (var1) {
    console.log('var1 is not null');
} else {
    console.log('var1 is null');
}

'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// Declare a boolean variable isAge and initialized
let isAge = false;
// Read the assign value
console.log(isAge);
// Reassigned the value to boolean variable
isAge= true;

if (isAge) {
  console.log('You can drive!');
} else {
  console.log("Sorry you can't drive, you are not old enough");
}
